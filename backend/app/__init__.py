"""
AI Interview Coach — FastAPI Backend
=====================================
Package initialiser for the app module.
Exposes nothing at the package level; it simply marks this directory as a
Python package so all sub-modules can be imported with the `app.` prefix.
"""
