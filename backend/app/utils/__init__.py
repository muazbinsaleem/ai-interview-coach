"""
app/utils/__init__.py
=====================
Package marker for the utils sub-package.

Small, reusable helper modules live here.  Nothing is re-exported at the
package level — import individual utilities directly from their modules.
"""
